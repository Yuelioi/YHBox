// Package calibration measures relative mouse counts for cross-device playback scaling.
//
// A calibration session listens to host mouse input while the user turns the target
// application through 360 degrees. Native input and global-hotkey support are provided
// by host adapters; state snapshots remain platform-neutral.
package calibration
