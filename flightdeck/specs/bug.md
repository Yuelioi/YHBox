---
status: idea
summary: 临时 bug 暂存池（不值得开 spec 的随手记）— 当前空
---

# 临时 bug 池（不进 spec）

随手记的临时 bug：**不值得单开一个 spec、但别丢**的就堆这里。
修掉一条就删一条；哪条养大了（要设计/要拆步骤）再单独提成正式 spec，从这里搬走。

以后直接往下面糊一行就行（截图也可），不用管格式。

---

（当前无待办 bug）
