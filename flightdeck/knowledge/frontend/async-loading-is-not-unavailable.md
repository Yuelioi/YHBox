---
kind: trap
summary: 异步请求的 pending 状态不能复用“不可用/未找到”终态；不同延迟的数据源必须独立加载，否则慢请求会制造假错误并阻塞已有内容。
activation: symptom
read_when: 详情页打开时先闪“未找到/未打开/不可用”、已有数据被另一个慢 RPC 的 skeleton 挡住、或多个异步数据源共用一个 loading 标志时
recheck_when: 前端异步状态管理方案、Vue Suspense 使用方式或后端请求取消与错误返回契约变化时
---
# 异步加载中不等于资源不可用

当一个视图依赖多个异步数据源时，必须分别表达每条请求的 pending、success 和 unavailable/error 状态。初始化值为 null 只说明结果尚未到达，不能直接渲染成“窗口未开”“未找到”或“不可用”。

不要用一个 Promise.all 或共享 loading 标志把快速的本地记录和可能超时的外部资源解析绑在一起。快速数据应先独立呈现；慢请求只控制自己的状态区域。否则用户会同时看到错误文案和 skeleton，把中间态理解成最终失败，同时已有内容也被无关请求阻塞。

回归测试至少应锁定两点：

- pending 时显示明确加载反馈，且不会出现 unavailable 终态文案。
- 慢请求未完成时，已经返回的独立数据仍然可见和可操作。

如果请求结束后确实失败，再进入 unavailable/error 状态；需要诊断的链路还应保留真实错误原因，不能只用 null 静默折叠所有失败。
