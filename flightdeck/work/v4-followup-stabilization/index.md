# V4 后续稳定性收尾

## Goal

完成 2026-07-28 的 V4 产品反馈修复，并按 Authoring 分类逐项检查全部内置节点的显示、配置、编译、
运行和错误反馈；修复发现的问题后完成一次最终增量验收，使当前 V4 工作区可以稳定续用。

## Status

Open

## Current

2026-07-28 的阶段性产品修复已提交为 `1cb7885f`：包括单实例启动恢复、启动器编排、AI 密钥编辑、
工作流保存冲突反馈、Macro 滚轮与编辑入口、工作流首页默认管理视图等。

提交后的节点显示一致性修复已完成并纳入本轮检查点：147 个内置节点中缺少专属标题键的数据端口与信号改用
中英文公共语义标签，专属标题仍优先，稳定技术 ID 保留在悬浮提示；节点内联时长控件使用 `xs` 紧凑
尺寸。子图管理也已按频率重排：子图主行可拖到画布创建调用，保留可点击“调用”作为键盘替代；调用
位置、重命名、复制和删除收进行级菜单，画布全局“添加调用”入口移除。相关 46 个定向测试、前端类型
检查与 i18n 门禁已通过。最后一次跨范围 `task check` 尚未运行，留待补丁 schema 修复完成后统一执行。

AI 模型安装与测试的协议错误也已修复并纳入本轮检查点：设置保存 API base URL，运行时按显式协议拼接请求
路径；新增 OpenAI Chat Completions 普通生成 adapter，DeepSeek 官方 base URL 与 V4 模型名可直接使用。
404、认证、权限、限流、超时和服务端错误改为字段上下文明确的中文反馈。Chat Agent continuation 尚未
实现，因此该协议的工具调用声明在前后端禁用。`internal/ai`、`internal/services`、Go vet、10 个前端
AI 设置测试、类型检查、i18n 与 Wails 合同一致性检查均已通过。

AI 输出上限交互也已修复并纳入本轮检查点：关闭数字控件步长吸附，手输 `4096` 不再变成 `4097`；增加“不限”
开关，profile 中 `maxOutputTokens = 0` 表示不设置安装级上限，各 provider payload 在没有单次请求上限时
省略 token 限制。字段备注统一降为 11px 次级文字并缩短。相关 AI 定向测试现为 12 个。

新增节点首次保存的 `INVALID_FIELD` 已修复并纳入本轮检查点：作者补丁现使用独立的节点引用、Endpoint 和
Edge 类型，允许同一批次的 `$handle`，持久化 Source Endpoint 仍只接受正式 ID。回归覆盖同批新增节点、
设置配置与连接信号，受影响的 Workflow/Wails 合同已通过正式入口重新生成。保存失败横幅也不再把所有
错误称为“保存冲突”或直接展示内部码；字段、必填项、连线、节点和版本冲突分别给出下一步，未知内部码
只作为辅助信息。作者补丁、前端 46 个定向测试、相关 Go 包编译、类型检查、i18n、Workflow 与 Wails
合同一致性均已通过。

编辑器反馈生命周期已继续收口并纳入本轮检查点：保存失败不再同时写入持久横幅、运行 Toast 与通用会话错误，
关闭横幅后不会重新露出 `INVALID_FIELD`。`PatchError` 通过统一 RPC 信封携带稳定 code 与
`commandIndex`；前端可据此说明具体字段并提供“定位问题”。未知大写错误码也只作为辅助诊断信息。
全仓 modal 已确认只通过 `BaseModal` 使用，并新增反馈表面约束测试与项目规则。相关 Go 定向测试、
79 个前端用例、类型检查和 i18n 门禁均已通过。

AI 测试结果与检查器字段层级已修复并纳入本轮检查点：成功测试只说明连接正常及可用模型，不再显示
`max-output` 等内部结束枚举；检查器和 AI 设置的解释性文字从同行 hint 改为 label 下方的 11px
description，字段名不再被长说明挤压，必填状态改用标准 `*`。相关 10 个前端用例、类型检查和 i18n
门禁已通过。

Log 节点手填消息无法编译的问题已修复并纳入本轮检查点：编译器不再强制解析未连接、未绑定的可选泛型输入，
因此 config 中的手填消息可以独立运行；一旦连接数据输入，类型仍须正常推导和校验，运行时继续由接线
值覆盖手填值。Program 重新打开、防伪校验、手填消息、字符串接线和可观察整数记录的定向测试均已通过。

AI 生成节点的 `所需目标不可用` 已修复并纳入本轮检查点：未评估模型可用于普通 Generate/Extract，评估门槛
继续用于 Agent、AI authoring 与工具权限，rejected/stale 模型仍不可运行；AI/HTTP/应用/自动化设置保存
后会原子发布完整运行环境，新 Run 无需重启即可使用。readiness 现在保留 requirement ID，并把缺少模型
或密钥直接说明为对应 AI 槽位。`internal/ai`、`internal/appbootstrap`、`internal/services`、
`internal/desktopapp` 包测试、前端定向测试和类型检查已通过。

诊断面板的错误卡片布局也已修复并纳入本轮检查点：移除大屏固定三列，所有严重级别与诊断卡片使用完整
横向空间，长标题自然换行。面板定向测试 9 个通过。

以上 V4 检查点已提交为 `a7ebfe01`。提交后已完成 AI 分类审查但尚未提交：内置受限 Agent 节点及其
固定单工具运行适配器已删除，AI 分类由 3 个节点精简为“生成文本”和“提取结构化数据”2 个；两者提示词
改用多行编辑器。提取节点以字段名、类型、说明和可空开关替代手写 JSON Schema，后端确定性生成并校验
严格 schema。当前 Authoring Projection 为 146 个节点、21 个分类。`internal/ai`、`internal/nodes`、
`internal/noderuntime`、`internal/appbootstrap`、`internal/services` 定向测试，12 个前端用例、类型检查、
i18n、Impeccable 检测和合同一致性均已通过。

AI 分类随后补齐多模态输入但尚未提交：Generate/Extract 均增加可选 Image 端口，可直接连接捕获窗口或
加载图片输出；节点通过独立 blob-read requirement 读取 PNG，在本地限制大小与像素、缩放并编码为有界
JPEG，再分别生成 OpenAI Responses、OpenAI Chat Completions 和 Anthropic Messages 原生图像内容块。
模型不支持视觉时保留供应商失败，不会静默忽略图片。AI、节点和运行时定向 Go 测试、前端 AI 投影测试、
类型检查、i18n 与合同一致性均已通过。

AI 契约热更新造成的连锁假诊断也已修复但尚未提交：真实 `aitest` Source 中一个未填写提示词的旧
Extract 节点仍固定在上一版摘要，通用迁移器因节点未填完整而拒绝升级，继而把正常 `in` 端口误报为
`UNKNOWN_PORT`。契约迁移现只判断已有配置、绑定与拓扑能否保留，不再把“必填值尚未填写”混作不兼容；
编辑器在检查和运行前刷新 Authoring Projection，并将旧节点升级命令排在本轮用户修改之前。真实形状的
应用层回归、作者迁移测试、46 个编辑器用例、前端类型检查与格式检查均已通过；未运行全量门禁。

新建自动化节点保存失败也已修复但尚未提交：工作流顶部选择的 `window-target` 是本机 Host Profile
逻辑槽位，不要求工作流同时内嵌同名便携目标定义；schema 之前错误地强制两者一一对应，导致
`set-target-default` 与新建点击模板节点的补丁以 `INVALID_FIELD` 失败。现已移除该错误交叉约束，
保留槽位名称和便携定义各自的完整校验；schema 与 authoring 两层回归及相关包测试通过，未运行全量门禁。

Run 时间线缺少时间信息也已补齐但尚未提交：每条 journal 事件现在显示相对 Run 启动时间的毫秒级偏移
和本机绝对时间，完整日期时间保留在悬浮提示中，原有尝试次数降为次级信息。现有后端 `queuedAt` 与
`occurredAt` 已足够，不增加新的运行时采集。12 个时间线定向用例、前端类型检查、i18n、格式和界面
规则检测均已通过；未运行全量门禁。

Run 时间线的排障能力继续补齐但尚未提交：工作台可切换约 70vh 的放大视图，时间线可通过系统保存对话框
导出完整、格式化的 `yotta.run-timeline` JSON，不受当前分页限制；导出只包含 Run 投影和既有脱敏摘要。
点击模板节点会累计 `capture_ms`、`match_ms`、抓图字节数和次数并写入匹配/超时状态与 adapter action，
下一次导出的记录可以直接区分抓图链路和模板匹配耗时。后端导出回归、Wails 合同生成与一致性检查、
12 个前端时间线用例、类型检查、i18n、格式、界面规则检测及点击模板定向 Go 测试均已通过；未运行
全量门禁。

“捕鱼”导出已定位并修复点击模板性能回归但尚未提交：4.47 秒节点耗时中 `capture_ms=2926`、
`match_ms=505`、`captures=2`，循环本身只耗时 17ms。V4 provider 迁移把旧版内存 RGBA 匹配退化为
整屏 PNG 编码、分块传递和再解码，并把点击前重新定位的默认等待从 0 改为 200ms。模板匹配和画面观察
现请求原始 RGBA，Windows 不再额外复制整帧，匹配读取约从 127 个 64KiB 分块降至 8 个 1MiB 分块；
窗口截图导出仍保留 PNG，并改用快速压缩。默认 settle 恢复为 0，只在用户明确配置时二次抓屏。provider、
运行时和节点契约定向测试通过，生成合同已更新；需要用新构建在同一窗口真机复测，未运行全量门禁。

构建版本脚本的过期 V3.1 清场逻辑也已删除但尚未提交：`versions:check` 不再裸扫描任意源码中的
`3.1` 子串，毫秒时间 `03.184` 等普通数据不会阻断构建；无人引用的 `version:verify` 兼容别名同步
移除，发布标签核对脚本保留并修正用途文档。CLI 级回归覆盖任意源码文本，`cmd/yotta-versions`
全包测试和实际 `task versions:check` 均通过；未运行全量门禁。

键鼠宏编辑体验也已重构但尚未提交：元信息与动作序列保留在同一个编辑入口，动作列表移除固定最大高度并
填满模态框剩余空间。编辑器默认使用简易视图，将录制的按下、等待、松开投影为一次带持续时间的按键或
单击，原子动作仍是唯一持久化格式并可切回原子视图。新增指定行后插入、批量复制、批量上移/下移和整组
拖放；只有拖拽柄会启动拖放，避免表单控件与拖动争抢。轻微鼠标抖动仍识别为单击，实际位移保留为原子
拖拽。42 个相关前端用例、类型检查、i18n 和界面规则检测通过；未运行全量门禁。

NewAPI 连接失败已完成实网诊断但尚未修复反馈：相同密钥、模型和 payload 下，根地址追加
`/chat/completions` 会得到 HTTP 200 的管理站 HTML，而保留 `/v1` 前缀后的 `/v1/chat/completions`
在 Go 默认 UA 与 Yotta UA 下都成功。问题不是请求头封禁，而是 base URL 缺少供应商路径前缀；当前
JSON 解码失败被错误压成无 HTTP 状态的 `invalid-request`，前端又隐藏原始原因，导致“供应商拒绝请求”
这一误导文案。正确路径上的纯文本请求成功；同通道单次图片请求返回 HTTP 503“上游服务暂不可用”，
只能确认失败与请求头无关，尚不能证明该通道稳定支持视觉。

## Next

重新构建后在同一游戏窗口运行一次“捕鱼”，从放大的 Run 时间线导出 JSON，确认点击模板默认
`captures=1`，并比较 [`capture_ms` / `match_ms` 采集点](../../../internal/noderuntime/automation_template.go)
与上一份记录；真机性能确认后按 [计划](plan.md) 恢复 `event` 分类检查。开发阶段只做定向验证，
所有分类完成后再运行一次最终 `task check`。

## Progress

- 2026-07-28 建立阶段性交接：确认 `INVALID_FIELD` 不是应用槽位配置错误，而是作者补丁 schema 与执行器
  的临时句柄语义不一致；后端同场景保存通过，生成 schema 对
  `/commands/2/connect/edge/to/nodeId = "$node_launch"` 稳定复现失败。
- 2026-07-28 统一节点端口/信号显示回退规则并压缩内联时长编辑器；新增测试确保所有当前内置端口与信号
  均存在中英文显示标签，技术 ID 和持久化契约未改动。
- 2026-07-28 重排子图管理信息层级：直接拖放或行内按钮创建调用，低频定义操作与调用位置收进菜单；
  删除画布重复的“添加调用”入口，并保留自身调用/循环依赖拦截。
- 2026-07-28 修复 AI 模型安装与连接测试：将用户输入定义为 base URL，新增显式 OpenAI Chat
  Completions 普通生成 adapter 与协议路由，并把内部 `not-found` 等枚举转换成可操作的中文错误。
- 2026-07-28 修复 AI 输出 token 设置：关闭步长吸附，增加不限语义并贯通 profile/request payload；
  同时压缩字段备注文案和字号，恢复主次层级。
- 2026-07-28 修复新增节点首次保存：作者补丁合同允许 `$handle` 参与同批配置和连线，持久化 Source
  规则不变；保存错误统一转换为带原因和处理建议的正文，不再裸露 `INVALID_FIELD`。
- 2026-07-28 修复 AI 生成启动准入：普通生成不再被无 UI 入口的离线评估拦截，设置保存会热发布完整
  installation generation，缺少模型或密钥的运行提示直接指出 AI 槽位。
- 2026-07-28 收口编辑器错误反馈生命周期：同一保存异常只占用一条持久横幅，关闭后不再从通用状态
  复现；作者补丁错误提供结构化命令位置，字段错误可直接定位节点；新增全仓 modal/反馈表面约束。
- 2026-07-28 简化 AI 模型连接成功文案，并统一窄栏字段层级：解释性说明下置为紧凑次级文字，避免
  与字段名同排挤压。
- 2026-07-28 修复 Log 节点手填消息被 `UNRESOLVED_TYPE` 阻断：未使用的可选泛型输入不要求凭空
  推导类型，接线后的泛型输入仍执行完整类型解析和 Program 校验。
- 2026-07-28 将诊断面板从大屏三列改为全宽纵向布局，错误标题可换行，避免单条诊断只占可用宽度的
  三分之一。
- 2026-07-28 确定下一阶段按当前 Authoring Projection 的 21 个分类逐项检查全部 147 个内置节点，
  最终全量门禁延后到所有分类修复完成后统一运行。
- 2026-07-28 完成 AI 分类审查：删除无法形成独立价值的受限 Agent 节点；提示词改为多行编辑；结构化
  提取改为字段列表并由后端生成严格 schema。当前基线更新为 146 个节点，其中 AI 分类 2 个。
- 2026-07-28 为两个 AI 节点补齐可选 Image 输入，贯通 Blob 读取、本地有界缩放编码及三种供应商的
  原生多模态请求，使捕获窗口结果可直接用于游戏画面理解和结构化提取。
- 2026-07-28 实网对照确认 NewAPI 连接错误与 User-Agent 无关：API base URL 需要保留 `/v1` 前缀；
  同时定位到 HTTP 200 HTML 被错误归类为供应商拒绝请求的反馈缺陷。
- 2026-07-28 修复 AI 节点契约热更新后的 `NODE_CONTRACT_MISMATCH`/`UNKNOWN_PORT` 连锁假诊断：
  未填完的兼容节点也会升级摘要，检查和运行会刷新目录并先保存迁移命令，再报告真正缺失的字段。
- 2026-07-29 修复顶部默认目标导致新建自动化节点无法保存：本机逻辑槽位不再被错误要求引用工作流
  内嵌的便携目标定义；真实 `set-target-default + add-node` 回归和 schema/authoring 包测试通过。
- 2026-07-29 为 Run 时间线补充每条事件的毫秒级相对偏移与本机绝对时间，使节点开始和结束事件之间的
  停顿可以直接比较；不修改 journal 或运行时采集协议。
- 2026-07-29 为 Run 时间线增加完整 JSON 导出与放大视图，并让模板自动化记录抓图和匹配分段耗时；
  “捕鱼”导出确认点击模板约 4.47 秒，其中两次抓图约 2.93 秒、匹配约 0.51 秒。
- 2026-07-29 修复模板自动化抓图性能回归：恢复内存 RGBA 快路径、移除 Windows 重复整帧复制、放大
  匹配读取分块，并把默认重新定位等待恢复为 0；PNG 只保留给实际截图产物。
- 2026-07-29 删除 `yotta-versions` 中一次性 V3.1 裸字符串清场扫描和无人引用的兼容 Task 别名；
  当前版本门禁只校验有明确所有者的产品投影与长期架构边界。
- 2026-07-29 重构键鼠宏编辑器：列表填满可用高度，默认语义化展示按键与单击持续时间，保留原子视图；
  支持指定位置插入、整组批量复制/移动和专用拖拽柄，底层原子动作合同不变。
